// Configuracion del cliente. Centraliza flags de desarrollo que se activan/desactivan
// editando este archivo. No requiere reiniciar el servidor (recargar el navegador basta).

// Si esta en true, se muestran:
//   - Toggle "Ocultar mapa de calor (dev)" arriba a la izquierda
//   - Panel de coordenadas (mesh / piso / Y=3 / Y=6) abajo a la izquierda
// Cambiar a false para entregas / kiosko.
export const DEV_TOOLS_ENABLED = false;
